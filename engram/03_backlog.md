# Backlog — listBuddy

> Estados: Backlog → To Do → En progreso → En revisión QA → Done | Bloqueado
> Solo nerv-qa escribe "Done". Mapear SIEMPRE el ID externo si existe.
> Niv (P-11, nivel de revisión QA): A=Advisory (default) · S=Strong · X=Adversarial.

## Sprint 0 — Alta del proyecto
| ID | Ext (ADO/Jira) | Tarea | Asignado | Estado | Niv | Criterios de aceptación | Rama |
|----|----------------|-------|----------|--------|-----|--------------------------|------|
| T-001 | — | [ADR] Relocate automático de archivos rotos en playlists de Traktor: definir estrategia de backup + escritura atómica del NML + criterio de matching (exacto vs. fuzzy) + cómo sincronizar `PRIMARYKEY KEY` de las playlists con el `LOCATION` reparado en `COLLECTION` | nerv-arquitecto | Done (QA 2026-07-24) | S | 1) ADR documenta estrategia de backup (copia previa del NML antes de escribir). 2) Define escritura atómica (write-to-temp + replace, nunca in-place directo). 3) Define criterio de matching por nombre de archivo/canción y desempate cuando hay >1 resultado (modal). 4) Cubre explícitamente la sincronización COLLECTION↔PLAYLISTS (PRIMARYKEY KEY). 5) No incluye implementación, solo decisión documentada en 02_architecture.md. | docs/adr-001-relocate-traktor → main |
| T-002 | — | Implementar F-07 (relocate Traktor) según ADR-001: encoder `path→(VOLUME,DIR,FILE)` en traktor_db.py + motor de relocate (backup, escritura atómica, matching, sync COLLECTION↔PLAYLISTS) + modal de desambiguación en UI + bloqueo si Traktor está abierto (R-03) | nerv-desktop | Done (QA 2026-07-24) | S | Ver ADR-001 en 02_architecture.md — Decisión, puntos 1-5. Implementado en commit 4c59c8d; probado con NML sintético, falta validar con NML real de Traktor. | feature/relocate-traktor → main |

| T-003 | — | Checkbox "Resolver automáticamente con la mejor coincidencia (sin preguntar)" en el flujo de relocate de Traktor (Addendum ADR-001, 2026-07-24): default OFF; cuando ON, aplica `candidates[0]` sin modal y loguea como "(auto)". Backup/escritura atómica sin cambios | nerv-desktop | Done (QA 2026-07-24) | S | 1) Checkbox visible junto al botón de relocate, default desmarcado. 2) Con checkbox OFF: comportamiento idéntico a T-002 (modal en >1 candidato). 3) Con checkbox ON: >1 candidato aplica el de mejor score sin modal, log marca "(auto)". 4) 0 candidatos sigue sin resolver igual. 5) Backup + write atómico sin cambios respecto a T-002. | feature/relocate-traktor → main |

| T-004 | — | **[BUG urgente]** `_location_to_path()` en traktor_db.py (línea 152-154) ignora `VOLUME` al reconstruir el path — en Windows resuelve DIR+FILE contra la unidad actual del proceso en vez de la unidad real (confirmado con NML real: entradas con `VOLUME="I:"` se resuelven mal si la app no corre desde I:). Esto hace que TODO el material en discos que no son la unidad actual se muestre "roto"/rojo, tanto antes como después de un relocate exitoso — resuelve D-01 en `main`. Ver T-005 para el mismo fix en la rama de relocate | nerv-desktop | Done (QA 2026-07-24) | A | 1) ✅ `_location_to_path` recibe `volume` y antepone la letra de unidad en Windows si matchea `^[A-Za-z]:$`. 2) ⚠️ PARCIAL: solo se actualizó el call site de traktor_db.py:~251 (`_build_track_index`, usado por preview/export en main). El call site traktor_relocate.py:129 NO existe en `main` — vive únicamente en `feature/relocate-traktor` (T-002, sin mergear). No se tocó esa rama por indicación explícita del ticket ("no es la misma rama de T-002/T-003"). **Pendiente:** aplicar el mismo fix a `find_broken_entries` en esa rama antes de mergear T-002/T-003, o va a reintroducir el bug. 3) ✅ Verificado con NML real del PO: `VOLUME="I:"` → `WindowsPath('I:/...')`; de 200 entradas con `VOLUME="I:"`, pasaron de 0/200 resueltas (antes) a 97/200 (después) — el resto son archivos genuinamente ausentes en disco, no relacionado al bug. 4) ✅ Encoder no tocado (no existe en main de todas formas). 5) No aplica en esta rama: no hay motor de relocate en main (vive en T-002, no mergeado). | fix/volume-blind-path-windows → main |

| T-005 | — | Reconciliar `fix/volume-blind-path-windows` (T-004) con `feature/relocate-traktor` (T-002/T-003, sin mergear): mergear el fix a la rama de relocate y aplicar el mismo patrón (pasar `volume` disponible en `loc.get("VOLUME","")`) a `find_broken_entries` en traktor_relocate.py:129, que hoy sigue llamando a la firma vieja de 2 argumentos | nerv-desktop | Done (QA 2026-07-24) | A | 1) `feature/relocate-traktor` tiene mergeado o rebaseado el fix de T-004. 2) `find_broken_entries` pasa `volume` a `_location_to_path` con la firma nueva de 3 argumentos. 3) Re-test contra el NML real del PO (solo lectura): confirmar que `find_broken_entries` ya no cuenta como "rotos" los 97/200 que el fix de T-004 resuelve. 4) No re-touchear backup/escritura atómica/checkbox de T-002/T-003. | feature/relocate-traktor → main |

| T-009 | — | Implementar F-08 (relocate Rekordbox 6) según ADR-002 (aceptado, PO aprobó retención N=5 y trade-off ANLZ no-transaccional): capa nueva `rekordbox_relocate.py`, backup del master.db, transacción única con `update_content_path()`, reuso del modal/checkbox de T-002/T-003, chequeo R-01 (RB cerrado) | nerv-desktop | Done (QA 2026-07-24) | S | Ver ADR-002 en 02_architecture.md — Decisión completa. No implementar sync de playlists (no aplica, Rekordbox liga por Content.ID). Reusar RelocateDialog/checkbox de auto-resolución ya existentes en ui_components.py/ui.py donde sea posible en vez de duplicar. | feature/relocate-rekordbox → main |

| T-006 | — | [ADR-002] Relocate para Rekordbox 6 (F-08): definir estrategia de backup del `master.db` (SQLCipher) antes de escribir, manejo del USN (`set_local_usn`) para no romper el sync con Rekordbox Cloud/streaming, uso de `pyrekordbox.update_content_path()` (ya actualiza FolderPath + ANLZ, pero no hace backup ni toca USN), y mismo criterio de matching/modal que Traktor (reusar diseño de ADR-001 donde aplique) | nerv-arquitecto | Done (QA 2026-07-24) | S | 1) ADR define estrategia de backup del master.db completo antes de cualquier escritura (SQLCipher, no es un XML — no se puede hacer atomic write parcial como con NML). 2) Define si hace falta `set_local_usn()` explícito o si `commit()` ya lo maneja vía SQLAlchemy — investigar antes de asumir. 3) Confirma que `update_content_path(check_path=True)` alcanza o si hace falta lógica extra para ANLZ huérfanos. 4) Reutiliza criterio de matching/modal de ADR-001 (nombre exacto, fuzzy solo rankea, checkbox opt-in de auto-resolución igual que T-003). 5) Cubre R-01 (Rekordbox debe estar cerrado — DB con lock de OS, distinto al caso Traktor). 6) Solo decisión, sin implementación. | docs/adr-002-relocate-rekordbox → main |

| T-007 | — | **[BUG + feature]** Concurrencia rota entre export y relocate + falta de cancelación/progreso durante la indexación de disco. Hallado por el PO en uso real: arrancó relocate, luego arrancó export mientras el relocate seguía indexando — no hubo aviso de "una cosa a la vez" y no quedó claro qué pasó con ninguno de los dos procesos | nerv-desktop | Done (QA 2026-07-24) | S | 1) `_start_export()` (ui.py:1736) debe chequear `self._relocate_worker.isRunning()` y bloquear con QMessageBox, igual que `_start_relocate()` ya hace al revés con `self._worker`. 2) Agregar botón "Cancelar" visible durante el relocate (mismo patrón de swap de label que `_cancel_export`/`export_btn`), conectado a `requestInterruption()`. 3) `build_basename_index()` en traktor_relocate.py debe chequear interrupción periódicamente DURANTE el os.walk (no solo después, en el loop de reparación) para que Cancelar corte también en medio de la indexación. 4) `build_basename_index()` debe emitir progreso incremental (ej. cada N archivos/carpetas) vía signal, para que la UI deje de mostrar "Indexando archivos en disco…" estático sin movimiento. 5) No se necesita ningún cambio de threading nuevo — las signals de Qt ya cruzan de worker-thread a UI-thread correctamente; el problema era la falta de guard + falta de progreso granular, no falta de multithreading. | fix/relocate-export-concurrency → main |

| T-008 | — | Al exportar, si algún track falla (no encontrado o error de copia), escribir un `.txt` con el detalle de fallas DENTRO de la carpeta de esa playlist (`dest_dir`), no solo en el log de la UI. worker.py ya acumula `missing_tracks` en una lista plana global — hay que agruparla por playlist para escribir un archivo por carpeta | nerv-desktop | Done (QA 2026-07-24) | A | 1) Si una playlist tuvo ≥1 falla (no encontrado o error de copia), se crea `dest_dir/_no_exportados.txt` (nombre con guion bajo para no mezclarse con el numerado de tracks). 2) Si no hubo fallas en esa playlist, no se crea el archivo (no ensuciar carpetas limpias). 3) Cada línea indica el track y el motivo exacto (no encontrado / error de copia + excepción). 4) El log de la UI se mantiene igual que hoy (no se saca nada, se suma el archivo). 5) No cambia el comportamiento de conteo (copied/missing/skipped totals). | fix/relocate-export-concurrency → main |

| T-010 | — | Header/título de ventana custom: reemplazar la barra de título nativa de Windows (minimizar/maximizar/cerrar + nombre, "muy de stock de sistema") por una acorde al estilo dark/light de la app (`styles.py`/qss) | nerv-desktop | **REVERTIDO 2026-07-24 — ver D-06** (Done de QA anulado, crasheaba la app) | A | 1) Ventana frameless con barra de título propia: botones minimizar/maximizar-restaurar/cerrar con el estilo visual de la app (no iconos nativos de Windows). 2) Arrastrar la barra mueve la ventana (drag-to-move). 3) Doble click en la barra maximiza/restaura. 4) Resize por los bordes sigue funcionando (frameless no debe romper el resize manual). 5) Probado en Windows (plataforma actual); dejar nota si algo es Windows-specific y podría no aplicar igual en macOS (la app también empaqueta para Mac, ver CLAUDE.md). | feature/custom-titlebar → main |

| T-014 | — | Iconografía de la app: el icon.ico/icns/preview existentes tenían el diseño (nota + flechas de sync entre librerías) en azul, sin relación con la paleta violeta (@{accent}/@{accent2}) del resto de la UI. Recoloreado por hue-shift selectivo preservando el concepto y las líneas cream; icon.ico ahora usa un glifo simplificado/más grueso en 16-32px (el line-art detallado se pierde a esos tamaños) y el diseño completo en 48px+ | Orquestador (directo) | Done | A | Ejecutado directamente, no requiere revisión QA — solo assets visuales, sin lógica. Commit bf48f28, rama feat/app-icon. | feat/app-icon → main |

| T-015 | — | Pase de diseño sobre el panel de playlists/preview: (1) botón "cerrar" por grupo en SALIDA·VISTA PREVIA que destilde el checkbox de esa playlist (equivalente a destildarla en el árbol), + un botón "cerrar todas" (puede reusar `_set_all(False)`). (2) La lista de playlists con checkboxes debe adaptarse mejor al espacio disponible, menos dependencia de scroll interno para librerías chicas/medianas. (3) Encontrar y corregir el botón "pause" que se ve celeste (no está en la paleta violeta actual, investigar antes de asumir cuál es) + mejorar contraste general de ese componente | nerv-desktop | En revisión QA (verificado por el Orquestador) | A | ✅ Verificado personalmente: botón ✕ por grupo destilda esa playlist correctamente (probado con WARMUP), "Cerrar todas" visible, cards más compactas, botón de pausa confirmado violeta (ya no celeste) en vivo reproduciendo un track. Causa raíz real de "celeste": el glyph ⏸ (U+23F8) lo renderiza Windows con Segoe UI Emoji (fuente a color COLR/CPAL) ignorando el `color` de QSS — fix correcto fue dibujar las barras con QPainter en vez de depender de la fuente de emojis. 1) Cada grupo en el preview tiene un botón ✕ que destilda esa playlist (misma acción que el checkbox, sin lógica nueva de "cerrado"). 2) Botón "cerrar todas" visible en el panel de preview. 3) Lista de playlists (checkboxes) usa mejor el espacio vertical disponible; scroll queda solo si la cantidad de playlists realmente no entra. 4) Botón "pause" identificado (lanzar la app y mirarlo, no adivinar) y recoloreado según @{accent}/@{accent2}; contraste del componente que lo contiene mejorado. | feature/ui-polish-preview → main |
| T-016 | — | Rehacer el header/barra de título custom (T-010, revertido por D-06) de forma SEGURA: frameless + resize por bordes usando `windowHandle().startSystemResize()` (API alto nivel de Qt6), NUNCA parsear structs nativos de Windows a mano con ctypes. Botones min/max/cerrar propios (ya existe el QSS `winctl_*`, reusar) | nerv-desktop | En revisión QA (verificado por el Orquestador) | S | ✅ Verificado personalmente por el Orquestador sobre `integration/ui-polish` (merge de chore/prod-hardening + feat/app-icon + feature/custom-titlebar-v2): app lanzada de verdad, sin `ctypes`/`nativeEvent` en el diff, ventana frameless con botones propios visibles, minimizar/maximizar/restaurar probados con clicks reales — proceso vivo y respondiendo &gt;1 minuto de interacción, sin crash. Falta: test de resize por arrastre real de mouse (no simulado) y botón cerrar. 1) Prohibido: `ctypes.wintypes`, `nativeEvent`, `MSG.from_address` o cualquier lectura de puntero nativo — es la causa exacta de D-06. 2) Resize por bordes vía `startSystemResize()` disparado desde `mousePressEvent`/hit-test en Python puro (sin ctypes). 3) Frameless + botones propios solo en Windows (`_IS_WINDOWS`), macOS sin tocar. 4) OBLIGATORIO antes de marcar terminado: lanzar `python main.py` de verdad (no alcanza con py_compile) y confirmar que abre, se puede mover/resizear/minimizar/maximizar/cerrar sin crashear — el Orquestador va a re-verificar esto personalmente antes de aceptarlo, por la lección de D-06. | feature/custom-titlebar-v2 → main |

| T-017 | — | **[Windows]** D-05: extraer el núcleo del loop `run()` duplicado entre `RelocateWorker` (Traktor) y `RekordboxRelocateWorker` (Rekordbox) a una base común (ej. `relocate_core.py`) — ANTES de aplicar T-018/T-019/T-020, que si no hay que escribirlos dos veces | nerv-desktop | Mergeado a main y pusheado (commit bc3399c) | S | 1) Un solo lugar define el orquestador del loop de resolución (0/1/N candidatos, auto_resolve, ask_user/modal, cancelación, progreso, logging). 2) `RelocateWorker`/`RekordboxRelocateWorker` heredan o delegan a esa base, sin duplicar el loop. 3) Comportamiento observable idéntico al actual (mismos logs, mismos signals) — esto es refactor puro, no cambia funcionalidad todavía. 4) Los 46 tests existentes siguen verdes. 5) No toca ADR-001/002 ni la UI. | fix/relocate-core-refactor → main |
| T-018 | — | **[Windows]** Pase de seguridad/UX sobre el relocate (post-refactor T-017), cubre B-2, B-3, I-1, I-4, I-5, I-6 del informe engram/07_production_readiness.md | nerv-desktop | Mergeado a main y pusheado (commit 3428ae6) — verificado en vivo por el Orquestador (diálogo de confirmación + advertencia de auto-resolución probados con clicks reales) | S | Ver detalle completo en engram/07_production_readiness.md secciones B-2, B-3, I-1, I-4, I-5, I-6. Resumen: confirmación previa a escribir (qué archivo, que hay backup, cerrar RB/Traktor), no dejar backup truncado ante ENOSPC + chequeo de espacio previo, avisar antes de cancelar/cerrar que se pierde el trabajo, re-chequear proceso corriendo justo antes de escribir (no solo al inicio), confirmación extra si auto-resolución está ON, mensajes de error sin exponer excepciones crudas de Python. | fix/relocate-core-refactor → main |
| T-019 | — | **[Windows]** B-4: botón "Restaurar copia de seguridad…" — listar backups existentes (fecha/hora + tamaño) y restaurar uno, para Traktor y Rekordbox | nerv-desktop | En progreso | S | Ver B-4 en engram/07_production_readiness.md. Ítem de mayor retorno del informe: el backup existe pero hoy es inalcanzable para un usuario no técnico. | fix/relocate-core-refactor → main |
| T-020 | — | **[Windows]** I-2: extender cancelación + progreso incremental a la fase de detección de rotos (`find_broken_entries`/`find_broken_content`), hoy solo cubierta en el walk de indexación (T-007) | nerv-desktop | En progreso | A | Ver I-2/I-3 en engram/07_production_readiness.md. Escenario real: disco de red o USB desconectado cuelga la detección minutos sin feedback ni forma de cancelar. | fix/relocate-core-refactor → main |
| T-021 | — | **[Windows]** I-8: chequeo de actualización simple (comparar `_APP_VERSION` contra GitHub releases al arrancar, aviso discreto con link, sin auto-updater) | nerv-desktop | Mergeado a main y pusheado (commit 5dae051) | A | Ver I-8 en engram/07_production_readiness.md. No bloquea, independiente del resto — puede correr en paralelo. | feat/update-check → main |
| T-022 | — | **[Windows]** Tests nuevos priorizados por el informe (engram/07_production_readiness.md, sección Testing): (1) backup interrumpido por OSError no debe dejar archivo parcial — el test que hubiera encontrado B-3, (2) aliasing en el índice inverso de Traktor (hallazgo adversarial), (3) opcional: unicode en nombres de archivo | sin asignar (nerv-desktop) | Bloqueado por T-018 | A | Los tests 1 y 2 validan directamente los fixes de T-018 — hacer junto o inmediatamente después. El de unicode es menor prioridad, ver informe. | fix/relocate-core-refactor → main |

| T-023 | — | **[SOLO EN MAC]** B-5: el encoder `path_to_location` (traktor_db.py, rama POSIX) hardcodea `volume = "Macintosh HD"` — no verificado nunca contra un NML real de macOS. Con el disco renombrado o el sistema en otro idioma, el relocate en Mac es un no-op silencioso que se reporta como éxito | sin asignar (PO, en su Mac) | To Do | S | Verificar el nombre real de VOLUME que usa Traktor en un NML real de Mac (probablemente vía `diskutil info /` o inspeccionando un NML existente si tenés uno). Corregir el encoder para derivarlo en vez de hardcodearlo. Re-test con NML real. | — |
| T-024 | — | **[SOLO EN MAC]** B-6: `is_traktor_running()` usa `pgrep -ix Traktor`, pero el proceso real en macOS probablemente se llama "Traktor Pro 3" (no matchea con `-x`, exact match). Sin este guard, Traktor pisa las reparaciones al cerrar y no hay backstop como en Rekordbox | sin asignar (PO, en su Mac) | To Do | S | Confirmar con Traktor abierto: `pgrep -ix Traktor` vs `pgrep -i traktor`. Ajustar el patrón de matching en traktor_relocate.py. | — |
| T-025 | — | **[SOLO EN MAC]** B-1 (parcial) + D-07: buildear `rb_exporter.spec` en Mac real y correr el checklist de 8 pasos de engram/07_production_readiness.md (sección B-1) — en particular el ítem 3 (escritura real de Rekordbox desde el bundle) y el ítem 6 (icon.icns, generado con Pillow sin iconutil, nunca abierto en un Mac real) | sin asignar (PO, en su Mac) | To Do | S | Ver checklist completo en engram/07_production_readiness.md B-1. Es el único ítem que puede invalidar todo lo demás si el bundle no arranca. | — |
| T-026 | — | **[SOLO EN MAC]** B-7 (macOS): firmar + notarizar el `.app` (Apple Developer Program, `notarytool` + `stapler`) — sin esto Gatekeeper no deja abrir el .app en la Mac de otra persona ("está dañado", mensaje engañoso). `entitlements.plist` ya existe (commit 754d69e) pero nunca se usó para firmar nada | sin asignar (PO, en su Mac) | To Do | A | Ver B-7 en engram/07_production_readiness.md. Requiere cuenta de Apple Developer (99 USD/año). | — |

## Veredictos QA
| Tarea | Veredicto | Defectos (si rechazo) | Fecha |
|-------|-----------|------------------------|-------|
| T-001 | APROBADO | — ADR-001 cubre los 5 criterios (backup, escritura atómica, matching+desempate, sync COLLECTION↔PLAYLISTS, solo decisión sin código). | 2026-07-24 |
| T-002 | APROBADO | — Motor verificado contra ADR-001 punto a punto en traktor_relocate.py: backup_collection (N=10), write_atomic (temp+fsync+replace), find_candidates+RelocateDialog, build_reverse_key_index/apply_relocation, is_traktor_running() bloqueando (R-03). PO validó con NML real. | 2026-07-24 |
| T-003 | APROBADO | — relocate_auto_chk default OFF confirmado (ui.py), rama OFF usa modal sin cambios, rama ON aplica candidates[0] con log "(auto)", 0 candidatos sigue sin resolver, backup/atómico intactos. | 2026-07-24 |
| T-004 | APROBADO | — `_location_to_path` antepone drive letter en Windows (regex `^[A-Za-z]:$`); el gap "PARCIAL" original (call site de traktor_relocate.py sin tocar) queda resuelto por el merge de T-005 en esta rama. | 2026-07-24 |
| T-005 | APROBADO | — `find_broken_entries` (traktor_relocate.py:131) usa la firma nueva de 3 args de `_location_to_path`, misma función ya validada en T-004 contra NML real (no una reimplementación). Nota: no hay evidencia en 05_handoff_log.md de un re-test explícito contra el NML real confirmando los 97/200 (criterio 3) — riesgo residual bajo por ser el mismo código ya probado; recomendado que el PO corra una verificación rápida antes del batch completo de 2090 links. | 2026-07-24 |
| T-006 | APROBADO | — ADR-002 cubre los 6 criterios (backup completo master.db, USN vía commit(autoinc=True) investigado contra fuente, check_path=True + ANLZ investigado, reusa matching/modal de ADR-001, cubre R-01, solo decisión). Adversarial ya corrido por nerv-arquitecto. | 2026-07-24 |
| T-007 | APROBADO | — `_start_export` bloquea si `_relocate_worker.isRunning()` (ui.py:1888); botón Cancelar simétrico conectado a `requestInterruption()`; `build_basename_index` chequea `should_stop()` por carpeta Y cada 250 archivos DURANTE el walk (no solo después); progreso incremental vía `on_progress`. | 2026-07-24 |
| T-008 | APROBADO | — `_no_exportados.txt` se crea por playlist con ≥1 falla y NO se crea si no hubo fallas (worker.py, `missing_by_dest`); líneas con motivo exacto; log de UI y conteos intactos. Deuda técnica registrada (D-03): en cancelación/disco lleno a mitad, `missing_by_dest` no se flushea (se escribe recién al final del loop completo) — el usuario igual ve cada falla en tiempo real en el log de la UI, pero pierde el .txt de playlists ya completadas antes de la interrupción. No bloquea: comportamiento ya divulgado por nerv-desktop como deliberado y consistente con el patrón preexistente del resumen de `missing_tracks`. | 2026-07-24 |
| T-009 | APROBADO | — rekordbox_relocate.py cumple ADR-002: backup N=5 antes del único write, siempre `db.commit()` (nunca `db.session.commit()`), batch con `commit=False` por pista + `db.rollback()` en fallo, ANLZ con try/except (AttributeError/FileNotFoundError/AssertionError/OSError), R-01 up-front vía `is_rekordbox_running()`, reusa `Candidate`/`RelocateRequest`/`build_basename_index`/`find_candidates`/`RelocateDialog`/checkbox sin duplicar. | 2026-07-24 |
| T-010 | APROBADO → **ANULADO 2026-07-24** | — Aprobado originalmente por QA vía `py_compile` + lectura de código, sin lanzar la app interactivamente. El PO reportó "no logro levantar la app" en la sesión siguiente: `nativeEvent()` hacía `ctypes.wintypes.MSG.from_address()` sobre un puntero nativo de Windows y crasheaba el proceso ENTERO (access violation) en el primer `show()` — sin ninguna excepción de Python atrapable, sin rastro en el log. Aislado por bisección con `python -c` (eliminando `nativeEvent` de la clase, la app arrancaba bien). Revertido por el Orquestador (commit `3daed1e`, rama `chore/prod-hardening`): vuelve al frame nativo de Windows. Ver D-06. **Lección de proceso:** un ticket que cambia window flags/eventos nativos debe verificarse lanzando la app de verdad, no alcanza con que compile. | 2026-07-24 |

## Deuda técnica
| ID | Descripción | Origen | Prioridad |
|----|-------------|--------|-----------|
| D-01 | `_location_to_path`/`_nml_key_to_path` en traktor_db.py ignoran `VOLUME` al reconstruir el path — correcto en macOS, puede dar falso "existe/no existe" en Windows si la librería no está en el mismo drive que el cwd de la app | T-002 (nerv-desktop), 2026-07-23 | **Confirmado en producción con NML real del PO 2026-07-24 → escalado a T-004** |
| D-02 | Rama macOS del nuevo encoder `path_to_location()` es best-effort sin verificar (no hay Mac disponible para confirmar formato real de VOLUME) | T-002 (nerv-desktop), 2026-07-23 | Media |
| D-03 | worker.py: `missing_by_dest` (T-008) solo se flushea a disco después de que el loop completo de exportación termina; una cancelación o disco lleno a mitad hace que se pierda el `_no_exportados.txt` de playlists ya completadas con fallas (no solo de la playlist interrumpida). Mitigado porque el log de UI ya muestra cada falla en tiempo real. Mejora sugerida: flushear por playlist o justo antes de cada `return` anticipado | T-008 (QA), 2026-07-24 | Baja |
| D-04 | ~~No hay suite de tests automatizados en todo el proyecto.~~ **PARCIALMENTE MITIGADA (chore/prod-hardening, 2026-07-24):** se agregó `tests/` con 46 tests de pytest sobre las piezas PURAS de correctitud que escriben sobre la librería del usuario — encoder/decoder de rutas de Traktor (round-trip Win), matching (find_candidates), backup+poda (N=10/N=5), escritura atómica (NML round-trip) y sync COLLECTION↔PLAYLISTS (apply_relocation, invariante anti-huérfano). **Pendiente:** sin tests de UI (Qt) ni contra DB/NML reales; el orquestador `run()` de ambos workers (loop de resolución) no está cubierto — ver D-05. La rama macOS del encoder sigue sin test contra NML real (D-02) | QA (auditoría integration/relocate-all), 2026-07-24 | Media |
| D-06 | T-010 (header custom) revertido — `nativeEvent()` con `ctypes.wintypes.MSG.from_address()` crasheaba el proceso completo al primer `show()` (access violation, sin traceback posible). Revertido a frame nativo de Windows (commit `3daed1e`). **Rehacer con un mecanismo seguro** si se quiere retomar: `self.windowHandle().startSystemResize(edge)` disparado desde `mousePressEvent` sobre el margen del borde, en vez de parsear structs Win32 a mano — evita todo el riesgo de puntero crudo | Orquestador (hotfix post-QA), 2026-07-24 | **Alta si se retoma** — de lo contrario, ninguna (ya reflejado en `main`/próximo merge sin la feature) |
| D-07 | `icon.icns` (T-014) fue escrito directo con `Image.save(..., format="ICNS")` de Pillow, sin pasar por `iconutil` (no disponible en esta máquina Windows) — no verificado abriendo el .app real en una Mac. Si algo se ve mal en el Dock/Finder de macOS, regenerar con `scripts/make_icon.py` (ya actualizado con la fuente correcta en `assets/listBuddyicon.png`) desde una Mac real | T-014, 2026-07-27 | Media (solo importa al empaquetar para Mac) |
| D-05 | Duplicación del orquestador `run()` entre `RelocateWorker` (Traktor) y `RekordboxRelocateWorker` (Rekordbox): el loop de resolución de candidatos (0/1/>1, auto_resolve, ask_user/modal, chequeo de cancelación, progreso, logging) es ~90% idéntico en ambos. El núcleo de matching YA está compartido (Candidate/RelocateRequest/build_basename_index/find_candidates importados de traktor_relocate por rekordbox_relocate), pero el loop no. Un bug futuro en la lógica de resolución hay que arreglarlo en dos lugares. Mitigación recomendada: extraer una base `_run_relocation_loop(resolver, applier)` o un mixin común, DESPUÉS de que haya tests del loop (hoy no los hay — ver D-04). No se refactorizó ahora para no tocar código recién aprobado por QA sin red de tests | Arquitecto (review prod-readiness), 2026-07-24 | Media |

## Histórico (sprints cerrados: 3 líneas c/u, máx. 5 sprints; el resto a ~/.nerv/archive/)
